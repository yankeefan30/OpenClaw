{
  "name": "polar-escalation-bench",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "node --check index.js && node --check bench.js && node --check mailbox.js && node --check probe.js && node --check ../RicoEscalationHandoff/result-contract.js",
    "test": "NODE_ENV=test node --test tests/*.test.mjs",
    "selftest": "node index.js selftest"
  }
}
