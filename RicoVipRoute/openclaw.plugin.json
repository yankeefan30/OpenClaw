{
  "id": "rico-vip-route",
  "name": "Rico VIP Route",
  "description": "ISTS/VIP direct iMessage turns start on Claude. Qwen is never the primary for those directs.",
  "version": "0.1.0",
  "configSchema": { "type": "object", "additionalProperties": false, "properties": {} },
  "activation": { "onStartup": true },
  "contracts": { "hooks": ["before_prompt_build", "before_agent_run"] }
}
