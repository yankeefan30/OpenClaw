{
  "id": "rico-vip-route",
  "name": "Rico VIP Route",
  "description": "ISTS/VIP direct iMessage turns start on Claude when the sender is a VIP handle. default:direct alone is not a VIP send.",
  "version": "0.1.1",
  "configSchema": { "type": "object", "additionalProperties": false, "properties": {} },
  "activation": { "onStartup": true },
  "contracts": { "hooks": ["before_prompt_build", "before_agent_run"] }
}
