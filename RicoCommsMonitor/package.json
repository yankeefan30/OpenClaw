{
  "name": "rico-comms-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "node --check monitor.js",
    "test": "NODE_ENV=test node --test tests/*.test.mjs"
  }
}
